# Python
__pycache__/
*.py[cod]
*$py.class

# 로컬 플레이 상태 (플레이마다 변경됨, 추적 제외)
core/save_data.json
core/save_slot.json
core/user_settings.json

# 빌드 산출물 (PyInstaller)
build/
dist/
*.spec

# 개발용 임시 파일
scratch/
test_log.txt

# OS
Thumbs.db
.DS_Store
desktop.ini

# Buildozer
!buildozer.spec
.buildozer/
bin/

# 안드로이드 서명 키 — 절대 커밋 금지 (분실 시 앱 업데이트 불가)
*.keystore
*.jks
release.keystore

# 스팀 로컬 테스트용 App ID 파일 — 커밋 금지 (앱마다 다름, 개발자 로컬에만)
steam_appid.txt
