"""게임 버전을 한 곳에서 관리한다.

배포할 때 이 파일의 VERSION만 올리면 화면 표시가 함께 바뀐다.

버전 규칙 (agent_rules.md 참고):
- 큰 업데이트(기능 추가 등):  1.0.4 → 1.0.5 처럼 마이너 번호를 올린다.
- 작은 핫픽스/버그 수정:      1.0.5 → 1.0.5.1 처럼 하위 번호를 붙인다.
- 개발/테스트 빌드:          1.0.5-dev.1 처럼 -dev 를 붙여 태그 → GitHub에 '프리릴리스'로 올라간다.

여기 VERSION은 '안정 채널' 표기용이다. Git 태그 이름이 실제 배포 채널(안정/프리릴리스)을 정한다.
"""

VERSION = "1.0.6-dev.18"


def display_version():
    return f"v{VERSION}"
